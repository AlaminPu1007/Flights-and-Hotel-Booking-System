import React from 'react';
import Counter from './Counter';

const page = () => {
    return (
        <div className='container'>
            <h1>Hello, Dashboard Page!</h1>
            <Counter />
        </div>
    );
};

export default page;
